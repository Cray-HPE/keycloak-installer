@Library('dst-shared@release/shasta-1.3') _

dockerBuildPipeline {
 app = "keycloak-setup"
 name = "cms-keycloak-setup"
 description = "Cray management system Keycloak setup utility"
 repository = "cray"
 imagePrefix = "cray"
 product = "shasta-standard,shasta-premium"
}
